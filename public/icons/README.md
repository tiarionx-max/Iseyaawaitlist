Small decorative marks exported from Figma, used via `components/ui/AccentMark.tsx`:

- `yellow-sparkle.png` — the sparkle flourish accent (`variant="sparkle"`)
- `yellow-chevrons.png` — the triple-chevron accent for dark sections (`variant="chevrons"`, default `tone="yellow"`)
- `green-chevrons.png` — a forest-green triple-chevron variant (`variant="chevrons" tone="green"`) for use on light backgrounds; not currently used anywhere on the page, kept here for whenever a light-background section needs the same accent.
